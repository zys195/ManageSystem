1. 本地推送到 GitHub

在本地执行：

cd D:\Programmer\FileManage
git push origin main
2. 登录服务器

ssh 用户名@服务器IP
3. 进入服务器项目目录

把路径换成你服务器真实项目路径：

cd /www/wwwroot/FileManage
4. 拉取最新代码

git pull origin main
确认最新提交包含：

git log -2 --oneline
应该能看到：

bb51f52 fix: repair docker deployment config text
23b404c feat: add backup and polish contract management
5. 检查 Docker Compose 配置

docker compose config
如果只提示 version is obsolete，可以忽略，不影响运行。

6. 重新构建并启动容器

推荐用这个：

docker compose up -d --build
这个命令会：

重新构建前端镜像
重新构建后端镜像
保留 PostgreSQL 数据卷 pgdata
保留 backend/uploads 上传文件目录
重启服务
注意：不要执行下面这个命令：

docker compose down -v
-v 会删除数据库 volume，数据会没。

7. 查看容器状态

docker compose ps
正常应该看到 db、backend、frontend 都在运行。

8. 查看日志

后端日志：

docker compose logs -f backend
前端日志：

docker compose logs -f frontend
数据库日志：

docker compose logs -f db
9. 如果只想重启，不重新构建

以后如果只是改了环境变量或者想重启：

docker compose restart
但这次你有代码更新和前端构建更新，所以应该用：

docker compose up -d --build
10. 验证页面

部署完成后访问你的服务器域名或 IP，检查：

数据备份页面是否正常
合同列表筛选是否正常
附件上传样式是否正常
中文乱码是否消失
合同状态逻辑是否正确
最简版命令就是：

cd /www/wwwroot/FileManage
git pull origin main
docker compose up -d --build
docker compose ps
docker compose logs -f backend